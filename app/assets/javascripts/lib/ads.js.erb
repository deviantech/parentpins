window.ppAds_Apply = function() {
  window['skimlinks'] && skimlinks();
};
  
<%# Pretty proud of this. Read the current skimlinks file on every asset precompile. It doesn't appear to change very often, and this saves us a network connection on every pageload (originally attempted to make async, but cross domain issues squashed that). %>
<%= open('http://s.skimresources.com/js/59265X1374391.skimlinks.js').read if Rails.env.production? || Rails.env.staging? || true %>
