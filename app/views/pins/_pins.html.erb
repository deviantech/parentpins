<% if @pins.blank? %>
  <div class="empty-results">No pins to show.</div>
<% else %>
  <ul class="timeline ajax-pagination" id="pins">
    <%= render @pins %>
  </ul>
  <%= pagination_link %>
<% end %>