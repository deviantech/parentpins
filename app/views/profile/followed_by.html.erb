<h1><span>&nbsp;</span><%= @profile.name %>'s Followers</h1>
  
<% if @profile.followed_by_even_indirectly_count.zero? %>
  <div class="empty_pins">
    <p><%= @profile.name %> isn't currently being followed by anyone.</p>
  </div>
<% else %>
  <div class="user_list">
    <ul class="users">
      <%= render @profile.followed_by_even_indirectly %>
    </ul>
  </div>
<% end %>
