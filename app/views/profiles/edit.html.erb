<div class="wrapper profile">

  <div class="content board board_wide">
    <%= form_for @profile, :url => profile_path(@profile), :html => {:class => "form narrow"} do |f| %>
      <h1>Edit Account</h1>

      <%= error_messages_for @profile %>

      <%= render 'form_fields', :f => f %>

      More... Avatars... Cover... Settings...

      <div class="submit">
        <%= f.submit 'Update Settings', :class => 'btn tertiary_action' %>
      </div>        
    <% end %>
  
  </div>

  <div class="clearfix"></div>

</div>