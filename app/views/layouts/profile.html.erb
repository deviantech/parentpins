<% provide :content do %>
  <%= render 'shared/profile_header' %>
  <div class="wrapper profile">

    <div class="info">

      <%= link_to image_tag(@profile.avatar.url, :alt => "#{@profile.name}'s avatar"), profile_path(@profile), :class => 'avatar' %>
      <h2><%= @profile.name %></h2>
      <%= render 'profile/follow_buttons', :to_follow => @profile %>      
      <ul class="nav_profile" data-profile-id="<%= @profile.id %>">
        <% if current_user == @profile %>
          <li><%= profile_nav_link :activity %></li>
        <% end %>
        <li><%= profile_nav_link :pins %></li>
        <li><%= profile_nav_link :boards %></li>
        <li><%= profile_nav_link :likes %></li>
        <li><%= profile_nav_link :following %></li>
        <li><%= profile_nav_link :followed_by %></li>
      </ul>

      <p class="kid_count">Has <%= @profile.kids.to_i.zero? ? 'no kids' : pluralize(@profile.kids, 'kid', 'kids') %></p>

      <% unless @profile.website.blank? %>
        <p><%= link_to 'External Website', @profile.website, :target => '_blank', :rel => 'nofollow' %></p>
      <% end %>
      
      
      <% if @profile.teacher? %>
        <p class="teacher_info teacher_icon">
          <%= display_teacher_info @profile %>
        </p>
      <% end %>

      <% unless @profile.bio.blank? %>
      <p class="bio"><%= @profile.bio %></p>
      <% end %>
    </div>

    <div class="profile_content_wrapper">
      <div class="content">
        <%= yield %>
      </div>
    </div>

    <div class="clearfix"></div>
  </div>
<% end %>

<%= render :template => 'layouts/application' %>