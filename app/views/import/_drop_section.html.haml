.drop_section_wrapper.clearfix{:data => {:extra_key => attribute_to_set}}
  %ul
    - droppables.each do |key, value|
      %li.drop-target{:data => {attribute_to_set => value, 'target-id' => value}}
        %h3= key
        %ul.collection
          = render :partial => 'pin', :collection => @pins_to_import.select{|p| p.send(attribute_to_set) == value }, :locals => {:extra_class => 'from-drop-target'}