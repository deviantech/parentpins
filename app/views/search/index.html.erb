<div class="wrapper">
  <div class="content board_wide">
    <h1>
      Search Results 
      <% unless params[:q].blank? %>
        for "<%= params[:q] %>"
      <% end %>
      (<%= @results.count %>)
    </h1>
    
    <div class="search_navigation">
      <%= search_links ['pins', 'boards', 'users'] %>
    </div>
    
    <% if @results.blank? %>
      <div class="empty-results">No <%= @kind.pluralize %> to show.</div>
    <% else %>
      <ul class="ajax-pagination <%= 'masonry' if @kind == 'pins' %> <%= search_classes_by_kind %>">
        <% @results.each do |r| %>
          <%= render :partial => partial_for_class(r), :object => r %>
        <% end %>
      </ul>
      
      <div class="clearfix"></div>
      <%= pagination_link %>
      <div class="clearfix"></div>
    <% end %>
  </div>
  <div class="clearfix"></div>
</div>

